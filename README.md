# Ncurses Bouncing Ball
## Description
A simple bouncing ball made with the ncurses library.
## Requirements
- C++20 capable compiler (clang or gcc)
- Ncurses library (libncurses-dev or ncurses)
## Build and Run
`
make build_db
./main
`